@{
    ViewData["Title"] = "ASP.NET Core + easyID";
}
<h2>Welcome @User.Claims.Where( x => x.Type == "name").FirstOrDefault().Value</h2>
 
<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>